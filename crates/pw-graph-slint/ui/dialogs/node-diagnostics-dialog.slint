import { DiagnosticsDialog } from "../components/diagnostics-dialog.slint";

export component NodeDiagnosticsDialog inherits DiagnosticsDialog {
    title: root.node-name + " · Identity";
    in property <string> node-name: "Node identity diagnostics";
}
