import { DiagnosticsDialog } from "../components/diagnostics-dialog.slint";

export component PatchbayDiagnosticsDialog inherits DiagnosticsDialog {
    title: "Patchbay diagnostics";
    copy-label: "Copy debug details";
}
